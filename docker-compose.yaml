services:
  app:
    build: .
    volumes:
      - .:/app
    working_dir: /app
    command: python polars_chunked.py optimal
    deploy:
      resources:
        reservations:
          cpus: '8.0'
          memory: 0.25g
        limits:
          cpus: '8.0'
          memory: 0.25g