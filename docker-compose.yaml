services:
  app:
    build: .
    volumes:
      - .:/app
    working_dir: /app
    command: python polars_chunked.py optimal
    deploy:
      resources:
        reservations:
          cpus: '18.0'
          memory: 0.25g
        limits:
          cpus: '18.0'
          memory: 0.25g