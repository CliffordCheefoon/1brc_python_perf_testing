services:
  app:
    build: .
    volumes:
      - .:/app
    working_dir: /app
    command: python polars_attempt.py
    deploy:
      resources:
        reservations:
          cpus: '1.0'
          memory: 1g
        limits:
          cpus: '1.0'
          memory: 1g