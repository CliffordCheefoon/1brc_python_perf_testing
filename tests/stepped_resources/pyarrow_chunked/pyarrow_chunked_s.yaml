services:
  app:
    build: ../../../
    volumes:
      - ../../../:/app
    working_dir: /app
    command: python pyarrow_chunked.py "pyarrow_chunked_s"
    deploy:
      resources:
        reservations:
          cpus: '1'
          memory: 1g
        limits:
          cpus: '1'
          memory: 1g