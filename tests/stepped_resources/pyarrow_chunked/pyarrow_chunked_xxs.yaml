services:
  app:
    build: ../../../
    volumes:
      - ../../../:/app
    working_dir: /app
    command: python pyarrow_chunked.py "pyarrow_chunked_xxs"
    deploy:
      resources:
        reservations:
          cpus: '0.25'
          memory: 0.25g
        limits:
          cpus: '0.25'
          memory: 0.25g