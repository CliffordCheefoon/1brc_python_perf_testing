services:
  app:
    build: ../../../
    volumes:
      - ../../../:/app
    working_dir: /app
    command: python pandas_chunked.py "pandas_chunked_xs"
    deploy:
      resources:
        reservations:
          cpus: '0.5'
          memory: 0.5g
        limits:
          cpus: '0.5'
          memory: 0.5g