services:
  app:
    build: ../../../
    volumes:
      - ../../../:/app
    working_dir: /app
    command: python pandas_chunked.py "pandas_chunked_m"
    deploy:
      resources:
        reservations:
          cpus: '2'
          memory: 2g
        limits:
          cpus: '2'
          memory: 2g