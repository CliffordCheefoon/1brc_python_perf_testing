services:
  app:
    build: ../../../
    volumes:
      - ../../../:/app
    working_dir: /app
    command: python polars_chunked.py "polars_chunked_m"
    deploy:
      resources:
        reservations:
          cpus: '2'
          memory: 2g
        limits:
          cpus: '2'
          memory: 2g